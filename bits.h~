#ifndef DECODE_BITS_H
#define DECODE_BITS_H

#include <stdio.h>
#include <math.h>
#include <limits.h>
#include <ctype.h>

void decode_bits(int ch);
int main();

#endif

#ifndef ENCODE_BITS_H
#define ENCODE_BITS_H

#include <stdio.h>
#include <math.h>
#include <limits.h>
#include<ctype.h>

void encode_bits(int ch);
int main();

#endif

